package com.eventticket.eventbooking.seat.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class RedisConfig {
     // Use default Spring Boot StringRedisTemplate - no custom bean needed
}
